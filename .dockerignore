# 构建上下文排除（减小镜像构建体积、加速缓存命中）

# Git
.git
.gitignore
.gitattributes

# 依赖目录（容器内重新安装，不用本地缓存）
frontend/node_modules
**/__pycache__
**/*.pyc

# 前端构建产物（Dockerfile 阶段1 会重新构建，不用本地 dist）
frontend/dist

# 本地运行数据（避免打进镜像）
data/netprobe.db
data/screenshots
output/

# IDE
.idea
.vscode
*.swp

# 文档（镜像不需要）
docs/
*.md
LICENSE

# Docker 自身（避免循环）
Dockerfile
docker-compose.yml
.dockerignore

# CI
.github/
